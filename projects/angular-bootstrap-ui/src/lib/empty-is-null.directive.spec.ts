import { EmptyIsNullDirective } from './empty-is-null.directive';

describe('EmptyIsNullDirective', () => {
  it('should create an instance', () => {
    const directive = new EmptyIsNullDirective();
    expect(directive).toBeTruthy();
  });
});
