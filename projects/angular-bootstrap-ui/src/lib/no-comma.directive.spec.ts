import { NoCommaDirective } from './no-comma.directive';

describe('NoCommaDirective', () => {
  it('should create an instance', () => {
    const directive = new NoCommaDirective();
    expect(directive).toBeTruthy();
  });
});
