import { BBChildDirective } from './bbchild.directive';

describe('BBChildDirective', () => {
  it('should create an instance', () => {
    const directive = new BBChildDirective();
    expect(directive).toBeTruthy();
  });
});
